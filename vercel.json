{
  "version": 2,
  "builds": [
    { "src": "src/server.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/upload/(.*)", "dest": "src/server.ts" },
    { "src": "/gateway/(.*)", "dest": "src/server.ts" }
  ]
}
